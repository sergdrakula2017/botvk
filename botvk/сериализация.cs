using System;
using System.Collections.Generic;
using System.Text;
using System.Runtime.Serialization.Json;
using System.IO;

namespace botvk
{
    class сериализация
    { /*
        //сериализация 
        FileStream stream = File.Create("test.json");
        DataContractJsonSerializer formatter = new DataContractJsonSerializer(фракция.GetType());
        formatter.WriteObject(stream, фракция);
            stream.Close();
    */
        }
}
